const numbers = [10, 20, 30, 40, 50, 60]
// forEach works like map method but it does not return an array or any result
// const result = numbers.forEach(n => console.log(n))
const result = numbers.forEach(n => {

})

console.log(result);
